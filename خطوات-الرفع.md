# رفع البوت على Railway

نظام الحفظ صار مثل البوت المرفق:

- قاعدة SQLite دائمة في `data/bot.sqlite3`.
- التعديلات تُكتب داخل Transaction، فلا يرجع البوت للإعدادات الصفرية بعد
  Restart أو Crash.
- في أول تشغيل، إذا وجد ملفات JSON القديمة داخل `data/`، يرحّلها تلقائياً إلى
  SQLite بدون حذفها.
- نسخة JSON احتياطية عند الإقلاع ثم كل 15 دقيقة.
- نحتفظ بآخر **4 نسخ فقط**؛ عند إنشاء النسخة الخامسة تُحذف أقدم نسخة تلقائياً.
- إذا تعطبت قاعدة SQLite، يحاول البوت استرجاع أحدث نسخة JSON سليمة تلقائياً.
- التكتات القديمة تبقى مفتوحة بعد عودة البوت، ولا تُغلق إلا بزر الإغلاق أو أمر `!close`.

## إعداد Railway

1. ارفع مجلد `merged-bot` إلى خدمة Railway.
2. أضف Variable باسم `TOKEN` وضع توكن Discord.
3. أضف Volume لخدمة البوت على Mount Path:
   ```
   /app/data
   ```
4. اترك `BOT_DATA_DIR` فارغاً؛ المسار الافتراضي هو `./data`، ويصبح داخل Railway
   `/app/data`. إذا استخدمت مساراً مختلفاً، عيّنه في `BOT_DATA_DIR`.
5. Deploy. راقب Logs حتى تظهر:
   ```
   💾 نظام الحفظ SQLite جاهز: .../data/bot.sqlite3
   🛟 تم حفظ نسخة احتياطية (startup): ...
   ✅ Bot is online: ...
   ```

## مهم

لا تشغل أكثر من نسخة فعالة من نفس البوت على Railway؛ SQLite وVolume محليان
وليسا قاعدة بيانات مشتركة بين نسخ متعددة. إعداد Railway يبقى على نسخة واحدة،
وهذا هو المناسب لهذا النظام ويمنع معالجة نفس التفاعل مرتين.